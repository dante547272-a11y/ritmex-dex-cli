# Ritmex Funding Monitor - Web版本

## 🚀 项目概述

成功将CLI版本的资金费率监控工具改造为Web界面！现在您可以通过浏览器访问，享受更直观的交互体验。

## ✨ 新功能特性

### 🎯 核心功能
- **实时数据展示**: 通过WebSocket实时更新所有交易所的资金费率数据
- **交易所筛选**: 可自由选择要对比的交易所（Lighter、Binance、Hyperliquid、EdgeX、GRVT、Aster、Backpack）
- **多维度排序**: 支持按代币符号或任意交易所的资金费率排序
- **价差排行榜**: 实时显示Top 10价差机会，支持自定义资本金额计算收益
- **响应式设计**: 完美适配桌面和移动设备

### 🎨 界面特色
- **现代化UI**: 采用渐变背景、毛玻璃效果的现代设计风格
- **实时状态**: 连接状态、刷新进度、错误信息一目了然
- **数据可视化**: 资金费率用颜色区分正负值，价差卡片展示套利机会
- **交互友好**: 表头点击排序、筛选器实时生效

## 🛠 技术架构

### 后端 (web-server.ts)
- **Bun服务器**: 使用Bun的原生HTTP和WebSocket支持
- **数据管理器**: `FundingDataManager`类负责：
  - EdgeX WebSocket连接（实时数据）
  - 定时拉取其他交易所数据（HTTP）
  - 数据标准化和合并处理
  - 实时广播给所有连接的客户端

### 前端 (web/)
- **index.html**: 结构化页面布局
- **style.css**: 现代化CSS样式，支持响应式设计
- **app.js**: 完整的前端逻辑
  - WebSocket客户端管理
  - 数据筛选和排序
  - 实时UI更新
  - 用户交互处理

## 🚀 启动方法

### 1. 安装Bun
```bash
curl -fsSL https://bun.sh/install | bash
# 重启终端或执行: source ~/.bashrc
```

### 2. 安装依赖
```bash
cd /path/to/ritmex-dex-cli
bun install
```

### 3. 启动Web服务器
```bash
# 方法1: 直接启动
bun web-server.ts

# 方法2: 热重载模式（开发用）
bun --hot web-server.ts
```

### 4. 访问界面
打开浏览器访问: http://localhost:3000

## 📱 使用说明

### 交易所筛选
- 勾选/取消勾选交易所复选框
- 只显示至少有2个选中交易所数据的合约
- 实时生效，无需刷新

### 排序功能
- 使用下拉菜单选择排序列和方向
- 或直接点击表头进行排序
- 支持升序/降序切换

### 显示设置
- **最大显示数量**: 控制表格显示的合约数量（5-100）
- **资本金额**: 设置用于计算价差收益的资金量

### 价差排行榜
- 显示前10个最大价差机会
- 包含高/低资金费率交易所信息
- 显示24小时预期收益率和绝对收益金额

## 🔧 配置说明

### 服务器配置
- **端口**: 默认3000，可在代码中修改
- **刷新间隔**: 各交易所数据刷新频率在`constants.ts`中定义
- **启用交易所**: 在`config.json`中配置

### 前端配置
- 所有设置都通过UI界面控制
- 设置会在用户会话中保持

## 🎯 主要改进

### 相比CLI版本的优势
1. **更好的可视化**: 表格、卡片、颜色编码
2. **更强的交互性**: 筛选、排序、实时设置
3. **更广的可访问性**: 任何设备的浏览器都能使用
4. **更佳的用户体验**: 无需学习命令行操作

### 性能优化
- WebSocket减少轮询开销
- 前端智能筛选减少数据传输
- 表格虚拟化支持大量数据显示

## 🐛 故障排除

### 服务器无法启动
1. 确保Bun已正确安装: `bun --version`
2. 检查端口3000是否被占用: `lsof -i :3000`
3. 查看错误日志确定具体问题

### 数据不更新
1. 检查网络连接
2. 查看浏览器控制台的WebSocket连接状态
3. 确认各交易所API可访问性

### 界面显示异常
1. 清除浏览器缓存并刷新
2. 检查浏览器控制台的JavaScript错误
3. 确保使用现代浏览器（支持WebSocket和CSS Grid）

## 📈 未来扩展计划

- [ ] 添加历史数据图表
- [ ] 支持更多交易所
- [ ] 添加价格提醒功能
- [ ] 支持数据导出
- [ ] 添加用户偏好设置持久化

## 🤝 贡献指南

欢迎提交Issue和Pull Request！

1. Fork项目
2. 创建功能分支
3. 提交更改
4. 发起Pull Request

---

🎉 **恭喜！您现在拥有了一个功能完整的Web版本资金费率监控工具！**
